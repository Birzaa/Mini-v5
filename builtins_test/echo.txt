// echo | cat -e
// echo il etait une fois | cat -e
// echo "il etait une fois " | cat -e
// echo 'il etait une fois ' | cat -e

// echo " " | cat -e			KO
// echo "" | cat -e				OK
// echo ' ' | cat -e			KO
// echo '' | cat -e				OK

// echo -n | cat -e									OK
// echo -n -n -n -n -n -n | cat -e					OK
// echo -n -n -n -n-n-n | cat -e					OK
// echo -n-n-n -n -n -n | cat -e					OK
// echo -n-n | cat -e								OK
// echo -n -n | cat -e								OK

// echo "-n" | cat -e								OK
// echo "-n"capart | cat -e							OK
// echo "-n" capart | cat -e						OK
// echo '-n' | cat -e								OK
// echo '-n'capart | cat -e							OK		
// echo '-n' capart | cat -e						OK

// echo "-n " | cat -e								OK
// echo "-n "capart | cat -e						OK
// echo "-n " capart | cat -e						OK
// echo '-n ' | cat -e								OK
// echo '-n 'capart | cat -e						OK
// echo '-n ' capart | cat -e						OK

// echo " -n " | cat -e								OK
// echo " -n "capart | cat -e						OK
// echo " -n " capart | cat -e						OK
// echo ' -n ' | cat -e								OK
// echo ' -n 'capart | cat -e						OK
// echo ' -n ' capart | cat -e						OK

// echo " -n" | cat -e								OK
// echo " -n"capart | cat -e						OK
// echo " -n" capart | cat -e						OK
// echo ' -n' | cat -e								OK
// echo ' -n'capart | cat -e						OK
// echo ' -n' capart | cat -e						OK

// echo -n-n-n-n-n-n | cat -e						OK
// echo "-n-n-n-n-n-n" | cat -e						OK
// echo '-n-n-n-n-n-n' | cat -e						OK


echo "capart "" " il etait une fois "" test1 '' 'test2' "'capart   '  " bonjour
echo -n "capart "" " il etait une fois "" test1 '' 'test2' "'capart   '" bonjour
echo -n "capart "" " il etait une fois "" test1 '' 'test2' "'capart'" bonjour
echo -n "capart "" " il etait une fois "" test1 '' 'test2' '"capart   "' bonjour
echo -n "capart "" " il etait une fois "" test1 '' 'test2' '"capart   "' bonjour
echo -n "capart "" " il etait une fois "" test1 '' 'test2' '"capart"' bonjour "'a'"'"a"'Test"a" "test"T

echo export Z="value" "capart "" " il etait une fois "" test1 '' 'test2' '"capart"' bonjour "'a'"'"a"'Test"a" "test"T


echo "test1"test2

echo "test1"test2 "'a'"b

echo "test1"test2 "'a'"a 'bonjour'"la famille"

echo "test1"test2 "'a'"a 'bonjour'"la famille" ca me casse les couilles "" ""
""	rien

" " espace

"a" "a"			a a
"a""a" 			aa

"'a'" "'a'" 	'a' 'a'
"'a'""'a'" 		'a''a'

'"a"' "'a'"		"a" 'a'
'"a"'"'a'"		"a"'a'

'"a"' '"a"'		"a" "a"
'"a"''"a"'		"a""a"

"'a'" '"a"' 	 'a' "a"
"'a'"'"a"'		'a'"a"



"'a'""'a'"

reccup le token double quote

'a'""'a'" 	